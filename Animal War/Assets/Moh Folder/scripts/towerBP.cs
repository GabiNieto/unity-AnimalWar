using UnityEngine;

public class towerBP : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    public float health;
    // Update is called once per frame
    void Update()
    {
        
    }
}
